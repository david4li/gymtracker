# Base project URL. Must NOT include /rest/v1 — supabase-js appends its own path.
SUPABASE_URL=https://uhqsokzxdsrkuyepyqhc.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Express backend. Server-only (no NEXT_PUBLIC_ prefix): the browser never
# calls it directly, so this must never reach the client bundle.
# Local:      http://localhost:4000
# Production: https://gymtracker-api.onrender.com
API_URL=http://localhost:4000
